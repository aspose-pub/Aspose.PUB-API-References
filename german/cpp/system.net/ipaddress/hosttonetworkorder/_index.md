---
title: "System::Net::IPAddress::HostToNetworkOrder Methode"
linktitle: "HostToNetworkOrder"
second_title: "Aspose.PUB für C++"
description: "System::Net::IPAddress::HostToNetworkOrder Methode. Konvertiert die angegebene Host-Byte-Reihenfolge in die entsprechende Netzwerk-Byte-Reihenfolge, in C++."
type: docs
weight: 1800
url: /de/cpp/system.net/ipaddress/hosttonetworkorder/
---
## IPAddress::HostToNetworkOrder(int16_t) method


Wandelt die angegebene Host‑Byte‑Reihenfolge in die entsprechende Netzwerk‑Byte‑Reihenfolge um.

```cpp
static int16_t System::Net::IPAddress::HostToNetworkOrder(int16_t host)
```


| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| host | int16_t | Der zu konvertierende Wert. |

### ReturnValue

Die entsprechende Netzwerk-Byte-Reihenfolge.

## Siehe auch

* Class [IPAddress](../)
* Namespace [System::Net](../../)
* Library [Aspose.PUB for C++](../../../)
## IPAddress::HostToNetworkOrder(int32_t) method


Wandelt die angegebene Host‑Byte‑Reihenfolge in die entsprechende Netzwerk‑Byte‑Reihenfolge um.

```cpp
static int32_t System::Net::IPAddress::HostToNetworkOrder(int32_t host)
```


| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| host | int32_t | Der zu konvertierende Wert. |

### ReturnValue

Die entsprechende Netzwerk-Byte-Reihenfolge.

## Siehe auch

* Class [IPAddress](../)
* Namespace [System::Net](../../)
* Library [Aspose.PUB for C++](../../../)
## IPAddress::HostToNetworkOrder(int64_t) method


Wandelt die angegebene Host‑Byte‑Reihenfolge in die entsprechende Netzwerk‑Byte‑Reihenfolge um.

```cpp
static int64_t System::Net::IPAddress::HostToNetworkOrder(int64_t host)
```


| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| host | int64_t | Der zu konvertierende Wert. |

### ReturnValue

Die entsprechende Netzwerk-Byte-Reihenfolge.

## Siehe auch

* Class [IPAddress](../)
* Namespace [System::Net](../../)
* Library [Aspose.PUB for C++](../../../)
