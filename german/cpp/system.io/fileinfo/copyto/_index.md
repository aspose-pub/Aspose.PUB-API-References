---
title: "System::IO::FileInfo::CopyTo-Methode"
linktitle: "CopyTo"
second_title: "Aspose.PUB für C++"
description: "System::IO::FileInfo::CopyTo-Methode. Kopiert die durch das aktuelle Objekt dargestellte Datei an den angegebenen Ort. Wenn die Zieldatei bereits existiert, schlägt das Kopieren in C++ fehl."
type: docs
weight: 300
url: /de/cpp/system.io/fileinfo/copyto/
---
## FileInfo::CopyTo(const String\&) method


Kopiert die durch das aktuelle Objekt repräsentierte Datei an den angegebenen Ort. Wenn die Zieldatei bereits existiert, schlägt das Kopieren fehl.

```cpp
FileInfoPtr System::IO::FileInfo::CopyTo(const String &destFileName)
```


| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| destFileName | const String\& | Der Zieldateiname |

### ReturnValue

Ein [FileInfo](../)-Objekt, das die Kopie darstellt

## Siehe auch

* Typedef [FileInfoPtr](../../../system/fileinfoptr/)
* Class [String](../../../system/string/)
* Class [FileInfo](../)
* Namespace [System::IO](../../)
* Library [Aspose.PUB for C++](../../../)
## FileInfo::CopyTo(const String\&, bool) method


Kopiert die durch das aktuelle Objekt repräsentierte Datei an den angegebenen Ort. Ein Parameter gibt an, ob die vorhandene Zieldatei überschrieben werden soll.

```cpp
FileInfoPtr System::IO::FileInfo::CopyTo(const String &destFileName, bool overwrite)
```


| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| destFileName | const String\& | Der Zieldateiname |
| überschreiben | bool | True, wenn die vorhandene Zieldatei überschrieben werden soll, false, wenn das Kopieren fehlschlagen soll, falls die Zieldatei bereits existiert |

### ReturnValue

Ein [FileInfo](../)-Objekt, das die Kopie darstellt

## Siehe auch

* Typedef [FileInfoPtr](../../../system/fileinfoptr/)
* Class [String](../../../system/string/)
* Class [FileInfo](../)
* Namespace [System::IO](../../)
* Library [Aspose.PUB for C++](../../../)
