---
title: "System::Drawing::Pens‑Klasse"
linktitle: "Pens"
second_title: "Aspose.PUB für C++"
description: "System::Drawing::Pens‑Klasse. Stellt eine Menge vordefinierter Pen‑Objekte bereit. Dies ist ein statischer Typ ohne Instanzdienste. Sie sollten niemals Instanzen davon auf irgendeine Weise in C++ erstellen."
type: docs
weight: 1600
url: /de/cpp/system.drawing/pens/
---
## Pens class


Stellt eine Menge vordefinierter [Pen](../pen/)-Objekte bereit. Dies ist ein statischer Typ ohne Instanzdienste. Sie sollten niemals Instanzen davon auf irgendeine Weise erstellen.

```cpp
class Pens
```

## Methoden

| Methode | Beschreibung |
| --- | --- |
| static [get_AliceBlue](./get_aliceblue/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FFF0F8FF hat. |
| static [get_AntiqueWhite](./get_antiquewhite/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FFFAEBD7 hat. |
| static [get_Aqua](./get_aqua/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FF00FFFF hat. |
| static [get_Aquamarine](./get_aquamarine/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FF7FFFD4 hat. |
| static [get_Azure](./get_azure/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FFF0FFFF hat. |
| static [get_Beige](./get_beige/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FFF5F5DC hat. |
| static [get_Bisque](./get_bisque/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FFFFE4C4 hat. |
| static [get_Black](./get_black/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FF000000 hat. |
| static [get_BlanchedAlmond](./get_blanchedalmond/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FFFFEBCD hat. |
| static [get_Blue](./get_blue/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FF0000FF hat. |
| static [get_BlueViolet](./get_blueviolet/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FF8A2BE2 hat. |
| static [get_Brown](./get_brown/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FFA52A2A hat. |
| static [get_BurlyWood](./get_burlywood/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FFDEB887 hat. |
| static [get_CadetBlue](./get_cadetblue/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FF5F9EA0 hat. |
| static [get_Chartreuse](./get_chartreuse/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FF7FFF00 hat. |
| static [get_Chocolate](./get_chocolate/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FFD2691E hat. |
| static [get_Coral](./get_coral/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FFFF7F50 hat. |
| static [get_CornflowerBlue](./get_cornflowerblue/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FF6495ED hat. |
| static [get_Cornsilk](./get_cornsilk/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FFFFF8DC hat. |
| static [get_Crimson](./get_crimson/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFDC143C hat. |
| static [get_Cyan](./get_cyan/)() | Gibt ein [Pen](../pen/)-Objekt mit einer Breite von 1 zurück, dessen ARGB‑Farbe in hexadezimaler Schreibweise den Wert #FF00FFFF hat. |
| static [get_DarkBlue](./get_darkblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF00008B hat. |
| static [get_DarkCyan](./get_darkcyan/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF008B8B hat. |
| static [get_DarkGoldenrod](./get_darkgoldenrod/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFB8860B hat. |
| static [get_DarkGray](./get_darkgray/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFA9A9A9 hat. |
| static [get_DarkGreen](./get_darkgreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF006400 hat. |
| static [get_DarkKhaki](./get_darkkhaki/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFBDB76B hat. |
| static [get_DarkMagenta](./get_darkmagenta/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF8B008B hat. |
| static [get_DarkOliveGreen](./get_darkolivegreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF556B2F hat. |
| static [get_DarkOrange](./get_darkorange/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFF8C00 hat. |
| static [get_DarkOrchid](./get_darkorchid/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF9932CC hat. |
| static [get_DarkRed](./get_darkred/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF8B0000 hat. |
| static [get_DarkSalmon](./get_darksalmon/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFE9967A hat. |
| static [get_DarkSeaGreen](./get_darkseagreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF8FBC8F hat. |
| static [get_DarkSlateBlue](./get_darkslateblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF483D8B hat. |
| static [get_DarkSlateGray](./get_darkslategray/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF2F4F4F hat. |
| static [get_DarkTurquoise](./get_darkturquoise/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF00CED1 hat. |
| static [get_DarkViolet](./get_darkviolet/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF9400D3 hat. |
| static [get_DeepPink](./get_deeppink/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFF1493 hat. |
| static [get_DeepSkyBlue](./get_deepskyblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF00BFFF hat. |
| static [get_DimGray](./get_dimgray/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF696969 hat. |
| static [get_DodgerBlue](./get_dodgerblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF1E90FF hat. |
| static [get_Firebrick](./get_firebrick/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFB22222 hat. |
| static [get_FloralWhite](./get_floralwhite/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFFAF0 hat. |
| static [get_ForestGreen](./get_forestgreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF228B22 hat. |
| static [get_Fuchsia](./get_fuchsia/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFF00FF hat. |
| static [get_Gainsboro](./get_gainsboro/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFDCDCDC hat. |
| static [get_GhostWhite](./get_ghostwhite/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFF8F8FF hat. |
| static [get_Gold](./get_gold/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFD700 hat. |
| static [get_Goldenrod](./get_goldenrod/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFDAA520 hat. |
| static [get_Gray](./get_gray/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF808080 hat. |
| static [get_Green](./get_green/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF008000 hat. |
| static [get_GreenYellow](./get_greenyellow/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFADFF2F hat. |
| static [get_Honeydew](./get_honeydew/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFF0FFF0 hat. |
| static [get_HotPink](./get_hotpink/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFF69B4 hat. |
| static [get_IndianRed](./get_indianred/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFCD5C5C hat. |
| static [get_Indigo](./get_indigo/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF4B0082 hat. |
| static [get_Ivory](./get_ivory/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFFFF0 hat. |
| static [get_Khaki](./get_khaki/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFF0E68C hat. |
| static [get_Lavender](./get_lavender/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFE6E6FA hat. |
| static [get_LavenderBlush](./get_lavenderblush/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFF0F5 hat. |
| static [get_LawnGreen](./get_lawngreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF7CFC00 hat. |
| static [get_LemonChiffon](./get_lemonchiffon/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFFACD hat. |
| static [get_LightBlue](./get_lightblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFADD8E6 hat. |
| static [get_LightCoral](./get_lightcoral/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFF08080 hat. |
| static [get_LightCyan](./get_lightcyan/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFE0FFFF hat. |
| static [get_LightGoldenrodYellow](./get_lightgoldenrodyellow/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFAFAD2 hat. |
| static [get_LightGray](./get_lightgray/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFD3D3D3 hat. |
| static [get_LightGreen](./get_lightgreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF90EE90 hat. |
| static [get_LightPink](./get_lightpink/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFB6C1 hat. |
| static [get_LightSalmon](./get_lightsalmon/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFA07A hat. |
| static [get_LightSeaGreen](./get_lightseagreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF20B2AA hat. |
| static [get_LightSkyBlue](./get_lightskyblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF87CEFA hat. |
| static [get_LightSlateGray](./get_lightslategray/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF778899 hat. |
| static [get_LightSteelBlue](./get_lightsteelblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFB0C4DE hat. |
| static [get_LightYellow](./get_lightyellow/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFFFE0 hat. |
| static [get_Lime](./get_lime/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF00FF00 hat. |
| static [get_LimeGreen](./get_limegreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF32CD32 hat. |
| static [get_Linen](./get_linen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFAF0E6 hat. |
| static [get_Magenta](./get_magenta/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFF00FF hat. |
| static [get_Maroon](./get_maroon/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF800000 hat. |
| static [get_MediumAquamarine](./get_mediumaquamarine/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF66CDAA hat. |
| static [get_MediumBlue](./get_mediumblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF0000CD hat. |
| static [get_MediumOrchid](./get_mediumorchid/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFBA55D3 hat. |
| static [get_MediumPurple](./get_mediumpurple/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF9370DB hat. |
| static [get_MediumSeaGreen](./get_mediumseagreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF3CB371 hat. |
| static [get_MediumSlateBlue](./get_mediumslateblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF7B68EE hat. |
| static [get_MediumSpringGreen](./get_mediumspringgreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF00FA9A hat. |
| static [get_MediumTurquoise](./get_mediumturquoise/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF48D1CC hat. |
| static [get_MediumVioletRed](./get_mediumvioletred/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFC71585 hat. |
| static [get_MidnightBlue](./get_midnightblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF191970 hat. |
| static [get_MintCream](./get_mintcream/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFF5FFFA hat. |
| static [get_MistyRose](./get_mistyrose/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFE4E1 hat. |
| static [get_Moccasin](./get_moccasin/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFE4B5 hat. |
| static [get_NavajoWhite](./get_navajowhite/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFDEAD hat. |
| static [get_Navy](./get_navy/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF000080 hat. |
| static [get_OldLace](./get_oldlace/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFDF5E6 hat. |
| static [get_Olive](./get_olive/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF808000 hat. |
| static [get_OliveDrab](./get_olivedrab/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF6B8E23 hat. |
| static [get_Orange](./get_orange/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFA500 hat. |
| static [get_OrangeRed](./get_orangered/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFF4500 hat. |
| static [get_Orchid](./get_orchid/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFDA70D6 hat. |
| static [get_PaleGoldenrod](./get_palegoldenrod/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFEEE8AA hat. |
| static [get_PaleGreen](./get_palegreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF98FB98 hat. |
| static [get_PaleTurquoise](./get_paleturquoise/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFAFEEEE hat. |
| static [get_PaleVioletRed](./get_palevioletred/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFDB7093 hat. |
| static [get_PapayaWhip](./get_papayawhip/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFEFD5 hat. |
| static [get_PeachPuff](./get_peachpuff/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFDAB9 hat. |
| static [get_Peru](./get_peru/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFCD853F hat. |
| static [get_Pink](./get_pink/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFC0CB hat. |
| static [get_Plum](./get_plum/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFDDA0DD hat. |
| static [get_PowderBlue](./get_powderblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFB0E0E6 hat. |
| static [get_Purple](./get_purple/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF800080 hat. |
| static [get_Red](./get_red/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFF0000 hat. |
| static [get_RosyBrown](./get_rosybrown/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFBC8F8F hat. |
| static [get_RoyalBlue](./get_royalblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF4169E1 hat. |
| static [get_SaddleBrown](./get_saddlebrown/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF8B4513 hat. |
| static [get_Salmon](./get_salmon/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFA8072 hat. |
| static [get_SandyBrown](./get_sandybrown/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFF4A460 hat. |
| static [get_SeaGreen](./get_seagreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF2E8B57 hat. |
| static [get_SeaShell](./get_seashell/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFF5EE hat. |
| static [get_Sienna](./get_sienna/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFA0522D hat. |
| static [get_Silver](./get_silver/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFC0C0C0 hat. |
| static [get_SkyBlue](./get_skyblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF87CEEB hat. |
| static [get_SlateBlue](./get_slateblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF6A5ACD hat. |
| static [get_SlateGray](./get_slategray/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF708090 hat. |
| static [get_Snow](./get_snow/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFFAFA hat. |
| static [get_SpringGreen](./get_springgreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF00FF7F hat. |
| static [get_SteelBlue](./get_steelblue/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF4682B4 hat. |
| static [get_Tan](./get_tan/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFD2B48C hat. |
| static [get_Teal](./get_teal/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF008080 hat. |
| static [get_Thistle](./get_thistle/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFD8BFD8 hat. |
| static [get_Tomato](./get_tomato/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFF6347 hat. |
| static [get_Transparent](./get_transparent/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #00FFFFFF hat. |
| static [get_Turquoise](./get_turquoise/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF40E0D0 hat. |
| static [get_Violet](./get_violet/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFEE82EE hat. |
| static [get_Wheat](./get_wheat/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFF5DEB3 hat. |
| static [get_White](./get_white/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFFFFF hat. |
| static [get_WhiteSmoke](./get_whitesmoke/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFF5F5F5 hat. |
| static [get_Yellow](./get_yellow/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FFFFFF00 hat. |
| static [get_YellowGreen](./get_yellowgreen/)() | Gibt ein [Pen](../pen/) Objekt mit einer Breite von 1 zurück und dessen ARGB-Farbe in hexadezimaler Schreibweise den Wert #FF9ACD32 hat. |
## Siehe auch

* Namespace [System::Drawing](../)
* Library [Aspose.PUB for C++](../../)
