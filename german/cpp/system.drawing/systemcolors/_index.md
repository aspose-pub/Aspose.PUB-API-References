---
title: "System::Drawing::SystemColors class"
linktitle: "SystemColors"
second_title: "Aspose.PUB für C++"
description: "System::Drawing::SystemColors class. Klasse, die eine Menge vordefinierter Color‑Objekte bereitstellt, die die Farben von Windows-Anzeigeelementen repräsentieren. Dies ist ein statischer Typ ohne Instanzdienste. Sie sollten unter keinen Umständen Instanzen davon in C++ erstellen."
type: docs
weight: 2600
url: /de/cpp/system.drawing/systemcolors/
---
## SystemColors class


Klasse, die eine Menge vordefinierter [Color](../color/)-Objekte bereitstellt, die die Farben von [Windows](../../system.windows/)-Anzeigeelementen repräsentieren. Dies ist ein statischer Typ ohne Instanzdienste. Sie sollten unter keinen Umständen Instanzen davon erstellen.

```cpp
class SystemColors
```

## Methoden

| Methode | Beschreibung |
| --- | --- |
| static [get_ActiveBorder](./get_activeborder/)() | Gibt ein [Color](../color/)-Objekt zurück, das die Farbe des Rahmens des aktiven Fensters darstellt. |
| static [get_ActiveCaption](./get_activecaption/)() | Gibt ein [Color](../color/)-Objekt zurück, das die Hintergrundfarbe der Titelleiste des aktiven Fensters darstellt. |
| static [get_ActiveCaptionText](./get_activecaptiontext/)() | Gibt ein [Color](../color/)-Objekt zurück, das die Textfarbe in der Titelleiste des aktiven Fensters darstellt. |
| static [get_AppWorkspace](./get_appworkspace/)() | Gibt ein [Color](../color/)-Objekt zurück, das die Farbe des Anwendungsarbeitsbereichs darstellt. |
| static [get_ButtonFace](./get_buttonface/)() | Gibt ein [Color](../color/)-Objekt zurück, das die Vordergrundfarbe eines 3‑dimensionalen Elements darstellt. |
| static [get_ButtonHighlight](./get_buttonhighlight/)() | Gibt ein [Color](../color/)-Objekt zurück, das die Hervorhebungsfarbe eines 3‑dimensionalen Elements darstellt. |
| static [get_ButtonShadow](./get_buttonshadow/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Schattens eines 3‑dimensionalen Elements darstellt. |
| static [get_Control](./get_control/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe der Fläche eines 3‑dimensionalen Elements darstellt. |
| static [get_ControlDark](./get_controldark/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Schattens eines 3‑dimensionalen Elements darstellt. |
| static [get_ControlDarkDark](./get_controldarkdark/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des dunklen Schattens eines 3‑dimensionalen Elements darstellt. |
| static [get_ControlLight](./get_controllight/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Lichts eines 3‑dimensionalen Elements darstellt. |
| static [get_ControlLightLight](./get_controllightlight/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Highlights eines 3‑dimensionalen Elements darstellt. |
| static [get_ControlText](./get_controltext/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Textes in einem 3‑dimensionalen Element darstellt. |
| static [get_Desktop](./get_desktop/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Desktops darstellt. |
| static [get_GradientActiveCaption](./get_gradientactivecaption/)() | Gibt ein [Color](../color/) Objekt zurück, das die hellste Farbe im Farbverlauf der Titelleiste eines aktiven Fensters darstellt. |
| static [get_GradientInactiveCaption](./get_gradientinactivecaption/)() | Gibt ein [Color](../color/) Objekt zurück, das die hellste Farbe im Farbverlauf der Titelleiste eines inaktiven Fensters darstellt. |
| static [get_GrayText](./get_graytext/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe von abgedunkeltem Text darstellt. |
| static [get_Highlight](./get_highlight/)() | Gibt ein [Color](../color/) Objekt zurück, das die Hintergrundfarbe der ausgewählten Elemente darstellt. |
| static [get_HighlightText](./get_highlighttext/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Textes der ausgewählten Elemente darstellt. |
| static [get_HotTrack](./get_hottrack/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe eines hot‑tracked Elements darstellt. |
| static [get_InactiveBorder](./get_inactiveborder/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Rahmens eines inaktiven Fensters darstellt. |
| static [get_InactiveCaption](./get_inactivecaption/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Hintergrunds der Titelleiste eines inaktiven Fensters darstellt. |
| static [get_InactiveCaptionText](./get_inactivecaptiontext/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Textes in der Titelleiste eines inaktiven Fensters darstellt. |
| static [get_Info](./get_info/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Hintergrunds eines ToolTips darstellt. |
| static [get_InfoText](./get_infotext/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Textes eines ToolTips darstellt. |
| static [get_Menu](./get_menu/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Hintergrunds eines Menüs darstellt. |
| static [get_MenuBar](./get_menubar/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Hintergrunds der Menüleiste darstellt. |
| static [get_MenuHighlight](./get_menuhighlight/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe zum Hervorheben von Menüeinträgen verwendet wird, wenn das Menü als Flachmenü angezeigt wird. |
| static [get_MenuText](./get_menutext/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Textes in einem Menü darstellt. |
| static [get_ScrollBar](./get_scrollbar/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Hintergrunds einer Bildlaufleiste darstellt. |
| static [get_Window](./get_window/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Hintergrunds im Client‑Bereich eines Fensters darstellt. |
| static [get_WindowFrame](./get_windowframe/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Fensterrahmens darstellt. |
| static [get_WindowText](./get_windowtext/)() | Gibt ein [Color](../color/) Objekt zurück, das die Farbe des Textes im Client‑Bereich eines Fensters darstellt. |
## Siehe auch

* Namespace [System::Drawing](../)
* Library [Aspose.PUB for C++](../../)
