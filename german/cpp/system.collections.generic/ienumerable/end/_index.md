---
title: "System::Collections::Generic::IEnumerable::end-Methode"
linktitle: "end"
second_title: "Aspose.PUB für C++"
description: "System::Collections::Generic::IEnumerable::end-Methode. Gibt einen Iterator zurück, der direkt nach dem letzten Element (falls vorhanden) der Sammlung zeigt. Dieser Iterator kann nicht verwendet werden, um ein referenziertes Objekt zu ändern, weil GetEnumerator() ein Kopie-Objekt von T in C++ zurückgibt."
type: docs
weight: 400
url: /de/cpp/system.collections.generic/ienumerable/end/
---
## IEnumerable::end() method


Gibt einen Iterator zurück, der direkt nach dem letzten Element (falls vorhanden) der Sammlung zeigt. Dieser Iterator kann nicht verwendet werden, um ein referenziertes Objekt zu ändern, weil [GetEnumerator()](../getenumerator/) ein Kopie-Objekt von T zurückgibt.

```cpp
iterator System::Collections::Generic::IEnumerable<T>::end()
```


### ReturnValue

Ein Iterator, der direkt nach dem letzten Element (falls vorhanden) der Sammlung zeigt

## Siehe auch

* Typedef [iterator](../iterator/)
* Class [IEnumerable](../)
* Namespace [System::Collections::Generic](../../)
* Library [Aspose.PUB for C++](../../../)
## IEnumerable::end() const method


Gibt einen Iterator zurück, der direkt nach dem letzten Element (falls vorhanden) der const-qualifizierten Instanz der Sammlung zeigt.

```cpp
const_iterator System::Collections::Generic::IEnumerable<T>::end() const
```


### ReturnValue

Ein Iterator, der direkt nach dem letzten Element (falls vorhanden) der const-qualifizierten Instanz der Sammlung zeigt

## Siehe auch

* Typedef [const_iterator](../const_iterator/)
* Class [IEnumerable](../)
* Namespace [System::Collections::Generic](../../)
* Library [Aspose.PUB for C++](../../../)
