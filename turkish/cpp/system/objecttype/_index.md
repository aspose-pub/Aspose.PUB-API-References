---
title: "System::ObjectType sınıfı"
linktitle: "ObjectType"
second_title: "Aspose.PUB için C++"
description: "System::ObjectType sınıfı. Nesne tipi alıcılarını uygulayan statik yöntemler sağlar. Bu, örnek hizmeti olmayan statik bir tiptir. C++'da hiçbir şekilde onun örneklerini oluşturmayınız."
type: docs
weight: 5000
url: /tr/cpp/system/objecttype/
---
## ObjectType class


Nesne türü alıcılarını uygulayan statik yöntemler sağlar. Bu, örnek hizmeti olmayan statik bir türdür. Herhangi bir şekilde onun örneklerini asla oluşturmamalısınız.

```cpp
class ObjectType
```

## Yöntemler

| Yöntem | Açıklama |
| --- | --- |
| static [GetType](./gettype/)(const T\&) | typeof() çevirisini uygular. Akıllı işaretçiler için aşırı yükleme. |
| static [GetType](./gettype/)(const T\&) | typeof() çevirisini uygular. Yapılar için aşırı yükleme. |
| static [GetType](./gettype/)(const T\&) | typeof() çevirisini uygular. İstisnalar için aşırı yükleme. |
| static [GetType](./gettype/)(const T) | typeof() çevirisini uygular. İlkel tipler için aşırı yükleme. |
| static [GetType](./gettype/)(const T) | typeof() çevirisini uygular. [Nullable](../nullable/) tipleri için aşırı yükleme. |
| static [GetType](./gettype/)() | typeof() çevirisini uygular. İlkel tipler için aşırı yükleme. |
| static [GetType](./gettype/)() | typeof() çevirisini uygular. Enum tipleri için aşırı yükleme. |
| static [GetType](./gettype/)() | typeof() çevirisini uygular. Yapılar ve işaretçiler için aşırı yükleme. |
| static [GetType](./gettype/)() | typeof() çevirisini uygular. [Nullable](../nullable/) için aşırı yükleme. |
| static [GetType](./gettype/)() | typeof() çevirisini uygular. MutlicastDelegate için aşırı yükleme. |
| static [GetType](./gettype/)() | typeof() çevirisini uygular. Yapılar ve işaretçiler için aşırı yükleme. |
| static [GetType](./gettype/)(const String\&) | typeof() çevirisini uygular. string türü için aşırı yükleme. |
| static [GetType](./gettype/)() | typeof() çevirisini uygular. uint8_t için aşırı yükleme. |
| static [GetType](./gettype/)() | typeof() çevirisini uygular. uint8_t için aşırı yükleme. |
| static [GetType](./gettype/)() | typeof() çevirisini uygular. uint8_t için aşırı yükleme. |
| static [GetType](./gettype/)() | typeof() çevirisini uygular. uint8_t için aşırı yükleme. |
| static [GetType](./gettype/)() | typeof() çevirisini uygular. uint8_t için aşırı yükleme. |
| static [GetType](./gettype/)() | typeof() çevirisini uygular. uint8_t için aşırı yükleme. |
## Ayrıca Bakınız

* Namespace [System](../)
* Library [Aspose.PUB for C++](../../)
