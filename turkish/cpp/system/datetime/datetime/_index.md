---
title: "System::DateTime::DateTime yapıcı"
linktitle: "DateTime"
second_title: "Aspose.PUB için C++"
description: "System::DateTime::DateTime yapıcı. C++'da MinValue'e eşit en küçük olası tarih ve saat değerini temsil eden bir örnek oluşturur."
type: docs
weight: 100
url: /tr/cpp/system/datetime/datetime/
---
## DateTime::DateTime() constructor


En küçük olası tarih ve saat değerini, MinValue'ye eşit olarak temsil eden bir örnek oluşturur.

```cpp
System::DateTime::DateTime()
```

## Ayrıca Bakınız

* Class [DateTime](../)
* Namespace [System](../../)
* Library [Aspose.PUB for C++](../../../)
## DateTime::DateTime(const DateTime\&) constructor


Bir örneği kopya-oluşturur.

```cpp
System::DateTime::DateTime(const DateTime &dt)=default
```


| Parametre | Tür | Açıklama |
| --- | --- | --- |
| dt | const DateTime\& | Temsil edilen tarih ve saat değerini kopyalamak için [DateTime](../) sınıfının bir örneği |

## Ayrıca Bakınız

* Class [DateTime](../)
* Class [DateTime](../)
* Namespace [System](../../)
* Library [Aspose.PUB for C++](../../../)
## DateTime::DateTime(int, int, int) constructor


Belirli bir yıl, ay ve gün olarak belirtilen bir tarih ve saat değerini temsil eden bir örnek oluşturur.

```cpp
System::DateTime::DateTime(int year, int month, int day)
```


| Parametre | Tür | Açıklama |
| --- | --- | --- |
| yıl | int | Oluşturulan örnek tarafından temsil edilecek yıl. |
| ay | int | Oluşturulan örnek tarafından temsil edilecek **year**'ın ayı. |
| gün | int | Oluşturulan örnek tarafından temsil edilecek **month**'ın günü. |

## Ayrıca Bakınız

* Class [DateTime](../)
* Namespace [System](../../)
* Library [Aspose.PUB for C++](../../../)
## DateTime::DateTime(int, int, int, const SharedPtr\<Globalization::Calendar\>\&) constructor


Belirtilen takvimde belirli bir yıl, ay ve gün olarak belirtilen bir tarih ve saat değerini temsil eden bir örnek oluşturur.

```cpp
System::DateTime::DateTime(int year, int month, int day, const SharedPtr<Globalization::Calendar> &calendar)
```


| Parametre | Tür | Açıklama |
| --- | --- | --- |
| yıl | int | Oluşturulan örnek tarafından temsil edilecek yıl. |
| ay | int | Oluşturulan örnek tarafından temsil edilecek **year**'ın ayı. |
| gün | int | Oluşturulan örnek tarafından temsil edilecek **month**'ın günü. |
| takvim | const SharedPtr\<Globalization::Calendar\>\& | Belirtilen **year**, **month** ve **day** değerlerini yorumlamak için kullanılan takvim. |

## Ayrıca Bakınız

* Typedef [SharedPtr](../../sharedptr/)
* Class [Calendar](../../../system.globalization/calendar/)
* Class [DateTime](../)
* Namespace [System](../../)
* Library [Aspose.PUB for C++](../../../)
## DateTime::DateTime(int, int, int, int, int, int) constructor


Belirli bir yıl, ay, gün, saat, dakika ve saniye olarak belirtilen bir tarih ve saat değerini temsil eden bir örnek oluşturur.

```cpp
System::DateTime::DateTime(int year, int month, int day, int hour, int minute, int second)
```


| Parametre | Tür | Açıklama |
| --- | --- | --- |
| yıl | int | Oluşturulan örnek tarafından temsil edilecek yıl. |
| ay | int | Oluşturulan örnek tarafından temsil edilecek **year**'ın ayı. |
| gün | int | Oluşturulan örnek tarafından temsil edilecek **month**'ın günü. |
| saat | int | Oluşturulan örnek tarafından temsil edilecek **day**'ın saati. |
| dakika | int | Oluşturulan örnek tarafından temsil edilecek **hour**'ın dakikası. |
| saniye | int | Oluşturulan örnek tarafından temsil edilecek **minute**'ın saniyesi. |

## Ayrıca Bakınız

* Class [DateTime](../)
* Namespace [System](../../)
* Library [Aspose.PUB for C++](../../../)
## DateTime::DateTime(int, int, int, int, int, int, const SharedPtr\<Globalization::Calendar\>\&) constructor


Belirtilen takvimde belirli bir yıl, ay, gün, saat, dakika ve saniye olarak belirtilen bir tarih ve saat değerini temsil eden bir örnek oluşturur.

```cpp
System::DateTime::DateTime(int year, int month, int day, int hour, int minute, int second, const SharedPtr<Globalization::Calendar> &calendar)
```


| Parametre | Tür | Açıklama |
| --- | --- | --- |
| yıl | int | Oluşturulan örnek tarafından temsil edilecek yıl. |
| ay | int | Oluşturulan örnek tarafından temsil edilecek **year**'ın ayı. |
| gün | int | Oluşturulan örnek tarafından temsil edilecek **month**'ın günü. |
| saat | int | Oluşturulan örnek tarafından temsil edilecek **day**'ın saati. |
| dakika | int | Oluşturulan örnek tarafından temsil edilecek **hour**'ın dakikası. |
| saniye | int | Oluşturulan örnek tarafından temsil edilecek **minute**'ın saniyesi. |
| takvim | const SharedPtr\<Globalization::Calendar\>\& | Belirtilen **year**, **month** ve **day** değerlerini yorumlamak için kullanılan takvim. |

## Ayrıca Bakınız

* Typedef [SharedPtr](../../sharedptr/)
* Class [Calendar](../../../system.globalization/calendar/)
* Class [DateTime](../)
* Namespace [System](../../)
* Library [Aspose.PUB for C++](../../../)
## DateTime::DateTime(int, int, int, int, int, int, DateTimeKind) constructor


Belirli bir yıl, ay, gün, saat, dakika ve saniye olarak belirtilen bir tarih ve saat değerini temsil eden bir örnek oluşturur.

```cpp
System::DateTime::DateTime(int year, int month, int day, int hour, int minute, int second, DateTimeKind kind)
```


| Parametre | Tür | Açıklama |
| --- | --- | --- |
| yıl | int | Oluşturulan örnek tarafından temsil edilecek yıl. |
| ay | int | Oluşturulan örnek tarafından temsil edilecek **year**'ın ayı. |
| gün | int | Oluşturulan örnek tarafından temsil edilecek **month**'ın günü. |
| saat | int | Oluşturulan örnek tarafından temsil edilecek **day**'ın saati. |
| dakika | int | Oluşturulan örnek tarafından temsil edilecek **hour**'ın dakikası. |
| saniye | int | Oluşturulan örnek tarafından temsil edilecek **minute**'ın saniyesi. |
| tür | DateTimeKind | Sağlanan tarih ve saat parametrelerinin yerel zaman, UTC zamanı veya hiçbirini belirtip belirtmediğini gösteren değer. |

## Ayrıca Bakınız

* Enum [DateTimeKind](../../datetimekind/)
* Class [DateTime](../)
* Namespace [System](../../)
* Library [Aspose.PUB for C++](../../../)
## DateTime::DateTime(int, int, int, int, int, int, int, const SharedPtr\<Globalization::Calendar\>\&, DateTimeKind) constructor


Belirtilen takvimde belirli bir yıl, ay, gün, saat, dakika, saniye ve milisaniye olarak belirtilen tarih ve saat değerini temsil eden bir örnek oluşturur.

```cpp
System::DateTime::DateTime(int year, int month, int day, int hour, int minute, int second, int millisecond, const SharedPtr<Globalization::Calendar> &calendar, DateTimeKind kind=DateTimeKind::Unspecified)
```


| Parametre | Tür | Açıklama |
| --- | --- | --- |
| yıl | int | Oluşturulan örnek tarafından temsil edilecek yıl. |
| ay | int | Oluşturulan örnek tarafından temsil edilecek **year**'ın ayı. |
| gün | int | Oluşturulan örnek tarafından temsil edilecek **month**'ın günü. |
| saat | int | Oluşturulan örnek tarafından temsil edilecek **day**'ın saati. |
| dakika | int | Oluşturulan örnek tarafından temsil edilecek **hour**'ın dakikası. |
| saniye | int | Oluşturulan örnek tarafından temsil edilecek **minute**'ın saniyesi. |
| milisaniye | int | Oluşturulan örnek tarafından temsil edilecek **second**'ın milisaniyesi. |
| tür | const SharedPtr\<Globalization::Calendar\>\& | Sağlanan tarih ve saat parametrelerinin yerel zaman, UTC zamanı veya hiçbirini belirtip belirtmediğini gösteren değer. |
| takvim | DateTimeKind | Belirtilen **year**, **month** ve **day** değerlerini yorumlamak için kullanılan takvim. |

## Ayrıca Bakınız

* Typedef [SharedPtr](../../sharedptr/)
* Class [Calendar](../../../system.globalization/calendar/)
* Enum [DateTimeKind](../../datetimekind/)
* Class [DateTime](../)
* Namespace [System](../../)
* Library [Aspose.PUB for C++](../../../)
## DateTime::DateTime(int, int, int, int, int, int, int, DateTimeKind) constructor


Belirli bir yıl, ay, gün, saat, dakika, saniye ve milisaniye olarak belirtilen bir tarih ve saat değerini temsil eden bir örnek oluşturur.

```cpp
System::DateTime::DateTime(int year, int month, int day, int hour, int minute, int second, int millisecond, DateTimeKind kind=DateTimeKind::Unspecified)
```


| Parametre | Tür | Açıklama |
| --- | --- | --- |
| yıl | int | Oluşturulan örnek tarafından temsil edilecek yıl. |
| ay | int | Oluşturulan örnek tarafından temsil edilecek **year**'ın ayı. |
| gün | int | Oluşturulan örnek tarafından temsil edilecek **month**'ın günü. |
| saat | int | Oluşturulan örnek tarafından temsil edilecek **day**'ın saati. |
| dakika | int | Oluşturulan örnek tarafından temsil edilecek **hour**'ın dakikası. |
| saniye | int | Oluşturulan örnek tarafından temsil edilecek **minute**'ın saniyesi. |
| milisaniye | int | Oluşturulan örnek tarafından temsil edilecek **second**'ın milisaniyesi. |
| tür | DateTimeKind | Sağlanan tarih ve saat parametrelerinin yerel zaman, UTC zamanı veya hiçbirini belirtip belirtmediğini gösteren değer. |

## Ayrıca Bakınız

* Enum [DateTimeKind](../../datetimekind/)
* Class [DateTime](../)
* Namespace [System](../../)
* Library [Aspose.PUB for C++](../../../)
## DateTime::DateTime(int64_t, DateTimeKind, bool) constructor


Tiks sayısı olarak belirtilen tarih ve saat değerini temsil eden bir örnek oluşturur. DAHİL KULLANIM İÇİN.

```cpp
System::DateTime::DateTime(int64_t ticks, DateTimeKind kind, bool is_ambiguous_local_dst)
```


| Parametre | Tür | Açıklama |
| --- | --- | --- |
| ticks | int64_t | Gürcü takviminde 1 Ocak 0001 00:00:00.000 tarihinden bu yana geçen 100 ns aralıklarının sayısı. |
| tür | DateTimeKind | **ticks** parametresinin yerel zaman, UTC zamanı veya hiçbirini belirttiğini gösteren değer. |
| is_ambiguous_local_dst | bool | Belirtilen tarih ve saat belirsizse ve birçok UTC zamanına eşlenebiliyorsa doğru. |

## Ayrıca Bakınız

* Enum [DateTimeKind](../../datetimekind/)
* Class [DateTime](../)
* Namespace [System](../../)
* Library [Aspose.PUB for C++](../../../)
## DateTime::DateTime(int64_t, DateTimeKind) constructor


Tiks sayısı olarak belirtilen tarih ve saat değerini temsil eden bir örnek oluşturur.

```cpp
System::DateTime::DateTime(int64_t ticks, DateTimeKind kind=DateTimeKind::Unspecified)
```


| Parametre | Tür | Açıklama |
| --- | --- | --- |
| ticks | int64_t | Gürcü takviminde 1 Ocak 0001 00:00:00.000 tarihinden bu yana geçen 100 ns aralıklarının sayısı. |
| tür | DateTimeKind | **ticks** parametresinin yerel zaman, UTC zamanı veya hiçbirini belirttiğini gösteren değer. |

## Ayrıca Bakınız

* Enum [DateTimeKind](../../datetimekind/)
* Class [DateTime](../)
* Namespace [System](../../)
* Library [Aspose.PUB for C++](../../../)
